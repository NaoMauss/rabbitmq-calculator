{
  "name": "rabbitmq-eval",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "sender": "bun run src/client/sender.ts",
    "receiver": "bun run src/client/receiver.ts",
    "worker:add": "bun run src/worker/addWorker.ts",
    "worker:sub": "bun run src/worker/subWorker.ts",
    "worker:mul": "bun run src/worker/mulWorker.ts",
    "worker:div": "bun run src/worker/divWorker.ts"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "amqplib": "^0.10.8",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^4.13.1",
    "@types/amqplib": "^0.10.7",
    "@types/bun": "latest",
    "eslint": "^9.27.0"
  }
}
